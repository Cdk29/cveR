---
title: "flat_cves-harvesting.Rmd empty"
output: html_document
editor_options: 
  chunk_output_type: console
---

```{r development, include=FALSE}
library(testthat)
```

```{r development-load}
# Load already included functions if relevant
pkgload::load_all(export_all = FALSE)
```

# cves_harvesting

```{r function-cves_harvesting}
#' cves_harvesting Title
#'
#' @return 1
#' @export
#'
#' @examples
cves_harvesting <- function() {
  1
}
```

```{r examples-cves_harvesting}
cves_harvesting()
```

```{r tests-cves_harvesting}
test_that("cves_harvesting works", {

})
```


```{r development-inflate, eval=FALSE}
# Run but keep eval=FALSE to avoid infinite loop
# Execute in the console directly
fusen::inflate(flat_file = "dev/flat_cves-harvesting.Rmd", vignette_name = "Go further")
```

